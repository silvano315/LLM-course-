[project]
name = "llm_course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "gitsource>=0.0.5",
    "loguru>=0.7.3",
    "minsearch>=0.1.0",
    "ollama>=0.6.2",
]
